{
  "name": "@jiki/content",
  "version": "1.0.0",
  "description": "Jiki blog posts and articles - markdown source files",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.0"
  }
}
