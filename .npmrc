# Enforce single lockfile for entire workspace
# Prevents pnpm from creating individual package lockfiles
shared-workspace-lockfile=true

# Hoist Sentry/OpenTelemetry instrumentation packages for Next.js compatibility
# Required for @sentry/nextjs to work with pnpm's strict dependency management
public-hoist-pattern[]=*import-in-the-middle*
public-hoist-pattern[]=*require-in-the-middle*
