import type { Extension } from "@codemirror/state";

export declare const colorScheme: Extension;
/**
 background, foreground, gutters, etc.
 */
export declare const EDITOR_COLORS: {
  background: string;
  foreground: string;
  caret: string;
  gutterBackground: string;
  gutterForeground: string;
  lineHighlight: string;
  selection: string;
  selectionMatch: string;
};
