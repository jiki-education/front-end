---
title: "Creating Functions with Inputs"
description: "Adding inputs (parameters) to your own functions so they can work with different data."
---

Parameters let your own functions accept inputs. When you define a function, you list the parameters it expects, and when you call it, you pass in the actual values.
