{
  "checks": {
    "rowMismatch": "The \"{{letter}}\" at square {{square}} should be \"{{expected}}\", not \"{{actual}}\".",
    "rowNotColored": "The row wasn't colored.",
    "rowWrongLength": "The row got {{actual}} states, but a row needs {{expected}}."
  },
  "tasks": {
    "processGuess": {
      "name": "Process a single guess",
      "description": "Create a function called processGuess that takes a target word and a guess, works out the state of each letter (correct, present, or absent), then calls colorRow(1, states) with the results."
    }
  },
  "scenarios": {
    "allCorrect": {
      "name": "All correct",
      "description": "Deal with a fully correct guess"
    },
    "absent": {
      "name": "Some absent",
      "description": "Handle when some letters are wrong"
    },
    "present": {
      "name": "Some present",
      "description": "Deal with letters in the wrong place"
    },
    "complex": {
      "name": "Complex",
      "description": "Deal with a more complex scenario"
    },
    "differentWord": {
      "name": "A different word",
      "description": "And finally a different word!"
    }
  },
  "hints": {
    "compareLetters": {
      "question": "How do I compare each letter of the guess to the target?",
      "answer": "Loop through the positions `0` to `4`. For each position, compare `guess[i]` with `target[i]`."
    },
    "stateRules": {
      "question": "When is a letter `\"correct\"` vs `\"present\"` vs `\"absent\"`?",
      "answer": "If `guess[i]` equals `target[i]`, it's `\"correct\"`. Otherwise, if the letter exists somewhere in `target`, it's `\"present\"`. If not, it's `\"absent\"`."
    },
    "buildList": {
      "question": "How do I build up the array of states?",
      "answer": "Start with an empty array, then use `states.push(value)` inside the loop to add each state onto the end in order."
    },
    "finishedList": {
      "question": "Where does the finished array go?",
      "answer": "Pass it to `colorRow(1, states)` to color the first row of the board with your computed states."
    }
  },
  "functions": {
    "colorRow": {
      "description": "Colors a row on the Wordle board with the given states.",
      "category": "Wordle"
    },
    "push": {
      "description": "Adds an element onto the end of an array, changing the array in place.",
      "category": "Arrays"
    },
    "includes": {
      "description": "Checks whether a string contains another string, returning true or false.",
      "category": "Strings"
    }
  },
  "describers": {
    "colorRow": "colored a row on the wordle board"
  }
}
