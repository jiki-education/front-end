# Helper function to check if an item is in a list might be useful

# The main function that gets called.
def count_nucleotides(dna):
    # Your code here
    # Initialize a dictionary with A, C, G, T all set to 0
    # Loop through each character in the DNA string
    # Check if it's a valid nucleotide
    # If valid, increment the count; if invalid, return False
    pass
