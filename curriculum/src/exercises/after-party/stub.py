# You might want to create helper functions first:
# - A function to check if a string starts with another string
#   (considering word boundaries)

def on_guest_list(names, person):
    # Go through each name in names
    # If the name starts with person's first name (followed by space or end), return True
    # If none of the names match, return False
    pass
