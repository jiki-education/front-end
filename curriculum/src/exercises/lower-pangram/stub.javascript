function includes(str, target) {
  // Check if the target character appears in str
  // Return true if found, false otherwise
}

function isPangram(sentence) {
  // Check if the sentence contains every letter a-z
  // Use your includes function to check each letter
}
