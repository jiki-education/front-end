// You might want to create helper functions first:
// - A function to calculate the length of a string
// - A function to remove the honorific from a name (everything before the first space)
// - A function to check if a string ends with another string

function on_guest_list with names, person do
  // Remove the honorific from person to get the surname
  // Go through each name in names
  // If the name ends with the surname, return true
  // If none match, return false
end
