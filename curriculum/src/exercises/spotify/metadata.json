{
  "slug": "spotify",
  "estimatedMinutes": 20,
  "levelId": "dictionaries",
  "hints": [
    "Start by using fetch() with the user endpoint URL, built by concatenating the base URL with the username",
    "Check if the response has an error key using hasKey() before processing items",
    "Loop through the items to extract artist API URLs, then fetch each one to get the artist name",
    "Build the sentence by joining artist names with commas, using ', and ' before the last one"
  ]
}
