function favoriteArtists(username) {
  // Your code here
  // 1. Build the API URL using the username
  // 2. Fetch the user's data
  // 3. Handle errors from the API
  // 4. Extract artist URLs from the response
  // 5. Fetch each artist's name
  // 6. Format and return the result sentence
}
