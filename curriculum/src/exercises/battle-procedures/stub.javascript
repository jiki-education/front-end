let leftBoundary = 0;
let rightBoundary = 10;
let direction = "right";
let position = 0;

// TODO: Create a shootIfAlienAbove function
// It should check if there's an alien above and shoot if so

repeat() {
  // TODO: Call your shootIfAlienAbove function here

  // TODO: Change direction when at a boundary

  // TODO: Move the laser in the current direction and update position
}
