function countWords(sentence) {
  // Your code here
  // 1. Convert the sentence to lowercase
  // 2. Extract individual words (letters, numbers, apostrophes)
  // 3. Count the frequency of each word using an object
  // 4. Return the object
}