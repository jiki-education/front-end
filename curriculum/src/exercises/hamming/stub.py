def hamming_distance(str1, str2):
    # Keep track of your position in the string
    # Keep track of the number of differences
    # Loop through each letter in str1
    # Compare with the letter at the same position in str2
    # Count the differences and return the total
    pass
