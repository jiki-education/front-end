// Write a helper function to check if a character is in a string

// The main function that gets called.
function count_nucleotide with strand, nucleotide do
  // Your code here
  // First check if the nucleotide is valid (one of A, C, G, T)
  // Then loop through each character in the strand
  // Check each character is valid, and count matches
end
