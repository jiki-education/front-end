// Position the Sun
// Use arithmetic and variables that refer to each other
// so the sun sits nicely in the top-right corner.
// When you change sunRadius, the position should adjust automatically.

// These are fixed
let canvasSize = 100;
let gap = 10;
let sunRadius = 15;
let skyColor = "skyblue";
let sunColor = "yellow";

// Derive these from the variables above
let sunX = 0;
let sunY = 0;

// Draw the Sky

// Draw the Sun
