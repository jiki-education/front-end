// Helper functions you might find useful:
// - contains(string, target): check if a string contains a character
// - index_of(string, target): find the position of a character in a string
// - to_lower(string): convert a string to lowercase

// The main function that gets called.
function is_pangram with sentence do
  // Your code here
end
