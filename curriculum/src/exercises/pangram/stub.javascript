// Helper functions you might find useful:
// - contains(str, target): check if a string contains a character
// - indexOf(str, target): find the position of a character in a string
// - toLower(str): convert a string to lowercase

// The main function that gets called.
function isPangram(sentence) {
  // Your code here
}
