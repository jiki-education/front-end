// Write helper functions: contains, isAlpha, isNumeric, isAlphanumeric
// Then use them in whatAmI to classify the string

function whatAmI(string) {
  // Use your helper functions to check the string
  // Return "Alpha", "Numeric", "Alphanumeric", or "Unknown"
}
