export const llmMetadata = {
  description:
    "The student must roll a golf ball into a hole by tracking its x position in a variable and using move_ball_to(x) inside a repeat loop. The ball starts at position 28 and must reach position 88.",
  tasks: {
    "roll-ball": {
      description:
        "Use a variable to track position, increment it in a loop, and pass it to move_ball_to(). The ball must end at position 88."
    }
  }
};
