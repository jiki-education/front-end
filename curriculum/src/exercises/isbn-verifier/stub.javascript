function isValidIsbn(isbn) {
  // Iterate through each character in the ISBN
  // Skip dashes, handle X as check digit (value 10)
  // Use a multiplier counting down from 10 to 1
  // Check if the total is divisible by 11
}