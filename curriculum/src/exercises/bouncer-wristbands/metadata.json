{
  "slug": "bouncer-wristbands",
  "estimatedMinutes": 10,
  "levelId": "conditionals",
  "hints": [
    "Start by storing the age in a variable, e.g. let age = getAge()",
    "Use if/else if/else to check the age ranges in order",
    "Check the youngest group first (age < 13), then work upwards",
    "The last group (seniors) doesn't need a condition — use else"
  ]
}
