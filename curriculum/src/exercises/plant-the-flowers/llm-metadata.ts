export const llmMetadata = {
  description:
    "The student must plant 9 flowers in a row by tracking position in a variable and using plant(position) inside a repeat loop. Flowers should be at positions 10, 20, 30, ..., 90.",
  tasks: {
    "plant-flowers": {
      description:
        "Use a variable starting at 10, increment by 10 each iteration, and call plant(position) inside a repeat loop. Must plant exactly 9 flowers."
    }
  }
};
