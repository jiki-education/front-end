import { IOExercise } from "../../IOExercise";
import metadata from "./metadata.json";

export default class WeatherSymbolsPart1Exercise extends IOExercise {
  static slug = metadata.slug;

  static availableFunctions = [];
}
